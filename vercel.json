{
  "version": 2,
  "installCommand": "bash -lc 'corepack enable && corepack prepare npm@11.6.2 --activate && npm ci'",
  "buildCommand": "bash -lc 'corepack enable && corepack prepare npm@11.6.2 --activate && cd packages/admin-web && npm run build'",
  "framework": null,
  "outputDirectory": "packages/admin-web/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.ts": {
      "runtime": "@vercel/node@3.2.0"
    }
  }
}
